import core from "./../coreBridge";
import { SampleInterface } from "../PlatformInterface";

export class MobileSample implements SampleInterface {
    sampleMethod(): void {
        // core.sample();
    }
}